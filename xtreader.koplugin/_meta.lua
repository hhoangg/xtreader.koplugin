local _ = require("gettext")
return {
    fullname = _("xtreader"),
    description = _([[Syncs books, reading progress and sleep-screen wallpapers with an xtreader server, so every reader on the account shares one library and one reading position.]]),
}
